{
	"name": "react-django-notes-app",
	"private": true,
	"scripts": {
		"start": "npm --prefix frontend start",
		"test": "npm --prefix frontend test",
		"build": "npm --prefix frontend run build"
	}
}
